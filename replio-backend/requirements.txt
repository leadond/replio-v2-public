# The canonical dependency list lives at the repository root, because that is
# the build root Railway uses. This include keeps local development working:
#   cd replio-backend && pip install -r requirements.txt
-r ../requirements.txt
