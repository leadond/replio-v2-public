# Repo-root requirements so Railpack detects this as a Python app when the
# build root is the repository root.
#
# Kept in sync with replio-backend/requirements.txt, which is the canonical list
# used for local development. An "-r replio-backend/requirements.txt" include was
# tried first but does not resolve inside the Railway build container.
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlmodel==0.0.22
psycopg2-binary==2.9.10
alembic==1.14.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
httpx==0.27.2
python-dotenv==1.0.1
pydantic-settings==2.6.1
tenacity==9.0.0
plivo==4.13.0
